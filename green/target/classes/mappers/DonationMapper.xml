<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="DonationDAO">

	<insert id="insertDona" parameterType="dona">
		INSERT INTO DONATION(
		don_name, don_ex, don_summary, don_point_sum, d_img1_nn)
		VALUES(
		#{don_name}, #{don_ex}, #{don_summary}, #{don_point_sum},#{d_img1_nn})
	</insert>

	<!-- 기부처 메인페이지  select one-->
	<select id="selectDona" resultType="dona">
		SELECT * FROM donation ORDER
		BY don_no
	</select>

	<!-- 기부처 상세페이지 -->
	<select id="getDona" parameterType="dona" resultType="dona">
		SELECT * FROM donation WHERE don_no=#{don_no}
	</select>

</mapper>